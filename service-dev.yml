apiVersion: v1
kind: Service
metadata:
  name: devops-lab-dev-service
  namespace: jenkins
spec:
  selector:
    app: devops-lab-dev
  ports:
    - protocol: TCP
      port: 3000      
      targetPort: 3000 
      nodePort: 31001  
  type: NodePort
